<% provide(:title, "Write a Review") %>

<%= form_for(@review, html: { multipart: true }) do |f| %>
	<%= f.label :review %><br />
  <%= f.text_area :rev_stmnt, placeholder: "Write your review here..." %><br/>
  <%= f.submit "Submit Review", class: "btn btn-primary" %>
<% end %>